 
 spawnMob(spawn_egg, location, number, text, potion_effects, items) {
 
 spawnItem(item, location, text) {
 
 wait(number) {
 
 createExplosion(location, number) {
 
 SpawnTnt(location, number, number) {
 
 launchFireworks(FIREWORK, location, text) {
 
 fireworkExplosion(FIREWORK, location) {
 
 fillHolder(location, items) {
 
 emptyHolder(location) {
 
 startLoop() {
 
 stopLoop() {
 
 setBlock(block, location, OPTIONAL LOCATION 2) {
 
 breakBlock(location) {
 
 copyBlocks(location, location, location, location) {
 
 createHologram(location, text) {
 
 removeHologram(LOCATION OR TEXT) {
 
 showScoreboard() {
 
 hideScoreboard() {
 
 setObjective(text) {
 
 setScore(text, number) {
 
 removeScore(text) {
 
 clearScoreboard() {
 
 enableBlockDrops() {
 
 disableBlockDrops() {
 
 cancelEvent() {
 
 spawnXpOrb(location, number, text) {
 
 spawnVehicle(VEHICLE, location, text, number) {
 
 changeSign(location, number, text) {
 
